# Segredos
agent/config.toml
lambda/.env
*.pem

# Build do Lambda
lambda/build/
lambda/*.zip

# Artefatos temporários do setup da AWS (ver docs/setup-aws.md)
trust.json
ssm-policy.json

# Python
__pycache__/
*.py[cod]
.venv/
venv/
